<br />
<div class="booyah-box col-xs-10 col-xs-offset-1">
  <div class="center_name"><%= current_drill.name %></div>
    <br class="clr" />
  <video id="MY_VIDEO_1" class="video-js vjs-default-skin vjs-big-play-centered" controls
    preload="auto" width="auto" height="auto" poster="/assets/black.jpg"
   data-setup="{}">
    <source src="<%= current_drill.video.to_s %>" type='video/mp4'>
   <p class="vjs-no-js">To view this video please enable JavaScript, and consider upgrading to a web browser that <a href="http://videojs.com/html5-video-support/" target="_blank">supports HTML5 video</a></p>
  </video>
  <br />
  <table class="table table-striped">
  <thead>
    <tr>
      <th>Drill Description</th>
      <th>Drill Pointers</th>
      <th colspan="<%= @colspan %>"></th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><%= current_drill.description %></td>
      <td><%= current_drill.pointers %></td>
    </tr>
  </table>
  <div class="center">
  <% if current_user.role == "admin" %>
  <br />
  <%= link_to 'Edit Drill', edit_drill_path(current_drill), class: 'btn btn-warning' %>
  <%= link_to 'Back', dashboard_admin_path, class: 'btn btn-success' %> 
  <% else %>
  <%= link_to 'Back', :back, class: 'btn btn-success' %> 
   <% end %>

</div>
</div>