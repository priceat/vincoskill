<br />
<div class="booyah-box col-xs-10 col-xs-offset-1">
  <h1>My Workouts</h1>
  <% if current_user.player? && current_user.team_id.present? %>
  <h2>Member of <% current_user.team.name %></h2>
  <% else %>
  <h2> You are not associated with a team! </h2>
  <% end %>
  <% if @workout.present? %>
  <table class="table table-striped">
  <thead>
    <tr>
      <th>Workout Name</th>
      <th>Completed?</th>
      <th>Date Started</th>
      <th>Date Finihed</th>
      <th>Time to Complete</th>
      <th>Rating</th>
      <th colspan="<%= @colspan %>"></th>
    </tr>
  </thead>
  </table>
  <% else %>
  <h3> You have not worked out! Get to it! </h3>
  <% end %>
  <%= link_to 'Get a New Workout!', workout_exercises_path, :method => :post, :class => 'btn btn-success btn-block' %>
  <%= link_to 'My Profile', edit_user_registration_path %>
</div>